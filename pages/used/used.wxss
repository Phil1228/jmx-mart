.placeholder { display:flex; flex-direction:column; align-items:center; justify-content:center; height:80vh; }
.ph-ico { font-size:120rpx; }
.ph-title { font-size:36rpx; font-weight:700; color:#1a1a1a; margin-top:24rpx; }
.ph-sub { font-size:26rpx; color:#999; margin-top:12rpx; }
